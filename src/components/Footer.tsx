import React from 'react';

export const Footer: React.FC = () => {
  // Footer is now integrated into the Contact section for a cleaner experience.
  // This component is kept for backwards compatibility.
  return null;
};
